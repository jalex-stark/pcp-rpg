# Core dependencies (lightweight baseline)
typer>=0.9.0
psutil>=5.9.0

# Dashboard (optional, for `make dashboard`)
fastapi>=0.104.0
uvicorn>=0.24.0

# LeanDojo integration (requires elan, git 2.25+, Python 3.9-3.12)
# Note: Python 3.13+ not supported yet - use Python 3.12 or earlier
# Install separately: python3.12 -m pip install lean-dojo>=2.0.0
# lean-dojo>=2.0.0; python_version<'3.13'

# Future: ReProver dependencies (uncomment for Profile 2)
# torch>=2.0.0
# transformers>=4.30.0
# faiss-cpu>=1.7.4

# Development
pytest>=7.4.0
